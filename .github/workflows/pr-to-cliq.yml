name: GitHub Notifications to Zoho Cliq

on:
  pull_request:
    types: [opened, closed, review_requested]
  push:
    branches:
      - main

jobs:
  send-to-cliq:
    runs-on: [self-hosted, on-premise]
    steps:      
      - name: Send Event Notification to Cliq
        env:
          CLIQ_WEBHOOK_URL: https://cliq.zoho.com/company/779974041/api/v2/channelsbyname/prtocliq/message?zapikey=1001.f9e733d0afd10db830565de7179030a3.a4d83ef0250bb0ff5135997314a39940
        run: |
          EVENT_TYPE=""
          TITLE=""
          URL=""
          AUTHOR=""
          MESSAGE=""

          if [ "${{ github.event_name }}" == "pull_request" ]; then
            URL="${{ github.event.pull_request.html_url }}"
            TITLE="${{ github.event.pull_request.title }}"
            AUTHOR="${{ github.event.pull_request.user.login }}"

            if [ "${{ github.event.action }}" == "opened" ]; then
              EVENT_TYPE="🚀 New Pull Request Opened"
              MESSAGE="*${EVENT_TYPE}*\n*Title:* ${TITLE}\n*Author:* ${AUTHOR}\n*Link:* ${URL}"

            elif [ "${{ github.event.action }}" == "closed" ]; then
              if [ "${{ github.event.pull_request.merged }}" == "true" ]; then
                EVENT_TYPE="✅ Pull Request Merged"
                MERGED_BY="${{ github.event.pull_request.merged_by.login }}"
                MESSAGE="*${EVENT_TYPE}*\n*Title:* ${TITLE}\n*Author:* ${AUTHOR}\n*Merged By:* ${MERGED_BY}\n*Link:* ${URL}"
              else
                EVENT_TYPE="❌ Pull Request Closed Without Merge"
                MESSAGE="*${EVENT_TYPE}*\n*Title:* ${TITLE}\n*Author:* ${AUTHOR}\n*Link:* ${URL}"
              fi

            elif [ "${{ github.event.action }}" == "review_requested" ]; then
              EVENT_TYPE="🔎 Review Requested on Pull Request"
              MESSAGE="*${EVENT_TYPE}*\n*Title:* ${TITLE}\n*Author:* ${AUTHOR}\n*Link:* ${URL}"
            else
              EVENT_TYPE="ℹ️ Unknown PR Event"
              MESSAGE="*${EVENT_TYPE}*\n*Title:* ${TITLE}\n*Author:* ${AUTHOR}\n*Link:* ${URL}"
            fi

          elif [ "${{ github.event_name }}" == "push" ]; then
            BRANCH_NAME="${{ github.ref }}"
            if [ "$BRANCH_NAME" == "refs/heads/main" ]; then
              # Get commit message directly from the event payload
              COMMIT_MESSAGE="${{ github.event.head_commit.message }}"
              
              # Check if this is NOT a PR merge by looking for PR pattern (#XXXX) or "Merge pull request"
              if ! echo "$COMMIT_MESSAGE" | grep -qE '\(#[0-9]+\)' && ! echo "$COMMIT_MESSAGE" | grep -qE 'Merge pull request #[0-9]+'; then
                # Additional check: if commit message contains "merge" or "Merge" (case insensitive) it's likely still a merge
                if ! echo "$COMMIT_MESSAGE" | grep -qiE '(^|\s)merge(\s|$)'; then
                  EVENT_TYPE="🚨 Direct Push to Main Detected"
                  AUTHOR="${{ github.actor }}"
                  COMMIT_URL="https://github.com/${{ github.repository }}/commit/${{ github.sha }}"
                  MESSAGE="*${EVENT_TYPE}*\n*Pushed By:* ${AUTHOR}\n*Commit:* ${COMMIT_URL}"
                fi
              fi
            fi
          fi

          if [ ! -z "$MESSAGE" ]; then
            curl -X POST "$CLIQ_WEBHOOK_URL" \
            -H "Content-Type: application/json" \
            -d "{
                  \"text\": \"$MESSAGE\"
                }"
          fi